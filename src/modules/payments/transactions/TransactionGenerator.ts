import { Payment, Transaction } from '../models/payment.interface';

// TODO Generate transactions
export class TransactionGenerator {
    public generateTransactions(payment: Payment): Transaction[] {
        return [];
    }
}
